[tool.poetry]
name = "poetry-test"
version = "0.1.0"
description = ""
authors = ["rtx"]

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
