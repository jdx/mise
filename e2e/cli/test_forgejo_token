#!/usr/bin/env bash

# Test mise forgejo token command

unset FORGEJO_TOKEN MISE_FORGEJO_TOKEN MISE_FORGEJO_ENTERPRISE_TOKEN MISE_FORGEJO_FJ_CLI_TOKENS

# Test 1: No token configured
assert_contains "mise forgejo token" "(none)"

# Test 2: FORGEJO_TOKEN env var
assert_contains "FORGEJO_TOKEN=env-test-token mise forgejo token --unmask" "env-test-token"
assert_contains "FORGEJO_TOKEN=env-test-token mise forgejo token --unmask" "FORGEJO_TOKEN"

# Test 3: MISE_FORGEJO_TOKEN takes precedence over FORGEJO_TOKEN
assert_contains "MISE_FORGEJO_TOKEN=mise-token FORGEJO_TOKEN=other mise forgejo token --unmask" "mise-token"
assert_contains "MISE_FORGEJO_TOKEN=mise-token FORGEJO_TOKEN=other mise forgejo token --unmask" "MISE_FORGEJO_TOKEN"

# Test 4: forgejo_tokens.toml
mkdir -p "$MISE_CONFIG_DIR"
cat >"$MISE_CONFIG_DIR/forgejo_tokens.toml" <<'EOF'
[tokens."codeberg.org"]
token = "toml-test-token"

[tokens."forgejo.example.com"]
token = "enterprise-toml-token"
EOF
assert_contains "mise forgejo token --unmask" "toml-test-token"
assert_contains "mise forgejo token --unmask" "forgejo_tokens.toml"
assert_contains "mise forgejo token forgejo.example.com --unmask" "enterprise-toml-token"

# Test 5: Env var beats toml file
assert_contains "FORGEJO_TOKEN=env-wins mise forgejo token --unmask" "env-wins"
assert_not_contains "FORGEJO_TOKEN=env-wins mise forgejo token --unmask" "toml-test-token"

# Test 6: fj CLI config fallback
rm "$MISE_CONFIG_DIR/forgejo_tokens.toml"
mkdir -p "$HOME/.local/share/forgejo-cli"
cat >"$HOME/.local/share/forgejo-cli/keys.json" <<'EOF'
{
  "hosts": {
    "codeberg.org": {
      "type": "Application",
      "name": "testuser",
      "token": "fj-cli-token"
    }
  }
}
EOF
assert_contains "mise forgejo token --unmask" "fj-cli-token"
assert_contains "mise forgejo token --unmask" "fj CLI"

# Test 7: fj CLI can be disabled
assert_contains "MISE_FORGEJO_FJ_CLI_TOKENS=false mise forgejo token" "(none)"

# Test 8: Token masking
TOKEN="a18099ca69064be387fbe37b8ad1d333758361f6"
assert_not_contains "FORGEJO_TOKEN=$TOKEN mise forgejo token" "$TOKEN"
assert_contains "FORGEJO_TOKEN=$TOKEN mise forgejo token" "a180"
assert_contains "FORGEJO_TOKEN=$TOKEN mise forgejo token" "61f6"

# Test 9: Enterprise env var for non-codeberg host
assert_contains "MISE_FORGEJO_ENTERPRISE_TOKEN=ent-token mise forgejo token forgejo.example.com --unmask" "ent-token"
assert_contains "MISE_FORGEJO_ENTERPRISE_TOKEN=ent-token mise forgejo token forgejo.example.com --unmask" "MISE_FORGEJO_ENTERPRISE_TOKEN"
