backends = ["github:raine/workmux", "cargo:workmux"]
bins = ["workmux"]
description = "git worktrees + tmux windows for zero-friction parallel dev"
test = { cmd = "workmux --version", expected = "workmux {{version}}" }
version_order = "semver"
