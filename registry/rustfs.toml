description = "High-performance, distributed, S3-compatible object storage system written in Rust"
test = { cmd = "rustfs -V", expected = "rustfs {{version}}" }
version_order = "source"

[[backends]]
full = "github:rustfs/rustfs"

[backends.options]
prerelease = true
