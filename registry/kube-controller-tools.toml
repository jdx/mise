description = "Tools to use with the controller-runtime libraries"
version_order = "source"
# test = { cmd = "controller-gen --version", expected = "Version: v{{version}}" }

bins = ["controller-gen"]
[[backends]]
full = "github:kubernetes-sigs/controller-tools"

[[backends]]
full = "asdf:jimmidyson/asdf-kube-controller-tools"
