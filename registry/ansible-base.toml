backends = ["pipx:ansible-base"]
description = "ansible-base is the predecessor of ansible-core, containing Ansible 2.10.x releases"
test = { cmd = "which ansible", expected = "ansible" } # ansible-base 2.10.x is incompatible with Python 3.12+, so ansible --version cannot be used
