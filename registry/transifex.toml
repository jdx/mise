description = "The Transifex command-line client"
# test = { cmd = "tx --version", expected = "version={{version}}" }

bins = ["tx"]
[[backends]]
full = "github:transifex/cli"

[[backends]]
full = "asdf:ORCID/asdf-transifex"
