backends = [
  { full = "github:jdx/tak", attestations_since = "0.0.2" },
  "cargo:tak-cli",
]
bins = ["tak"]
description = "CLI performance, tracked"
test = { cmd = "tak --version", expected = "tak {{version}}" }
version_order = "semver"
