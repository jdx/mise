backends = [
  "aqua:doitintl/kube-no-trouble",
  "asdf:virtualstaticvoid/asdf-kubent",
]
bins = ["kubent"]
description = "Easily check your clusters for use of deprecated APIs"
test = { cmd = "kubent --version", expected = "version {{version}}" }
version_order = "semver"
