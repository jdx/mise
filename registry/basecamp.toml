backends = [
  { full = "github:basecamp/basecamp-cli", attestations_since = "0.3.0" },
]
bins = ["basecamp"]
description = "Official command-line interface for Basecamp"
test = { cmd = "basecamp --version", expected = "basecamp version {{version}}" }
version_order = "semver"
