backends = [
  { full = "github:basecamp/hey-cli", attestations_since = "0.1.1-rc.2" },
]
bins = ["hey"]
description = "A CLI and TUI for HEY"
test = { cmd = "hey --version", expected = "hey version {{version}}" }
version_order = "semver"
