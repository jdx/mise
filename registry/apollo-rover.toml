backends = [
  { full = "github:apollographql/rover", attestations_since = "0.39.1-rc.1" },
]
bins = ["rover"]
description = "The CLI for Apollo GraphOS"
test = { cmd = "rover --version", expected = "Rover {{version}}" }
version_order = "semver"
