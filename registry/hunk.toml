aliases = ["hunkdiff"]
description = "Review-first terminal diff viewer for agentic coders"
test = { cmd = "hunk --version", expected = "{{version}}" }
version_order = "semver"

bins = ["hunk"]
[[backends]]
full = "aqua:modem-dev/hunk"

[[backends]]
attestations_since = "0.13.1"
full = "github:modem-dev/hunk"

[backends.options]
bin = "hunk"
