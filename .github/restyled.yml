enabled: true
restylers:
  - "!shellharden"
  # formatting is handled by oxfmt (see hk.pkl); disable restyled's prettier
  # so it doesn't fight oxfmt (notably the fig trailingComma=es5 override).
  - "!prettier"
  - "*"
