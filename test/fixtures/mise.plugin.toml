#:schema ../../schema/mise.plugin.json

[exec-env]
cache-key = ["{{'1234'}}"]

[list-idiomatic-filenames]
data = "test-idiomatic-filenames"

[package-manager]
requires = ["helm", "kubectl"]
supports-version-pins = true
os = ["macos", "linux"]
