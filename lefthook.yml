pre-commit:
  follow: true
  commands:
    pre-commit:
      run: just -v pre-commit
      interactive: true
    lint:
      run: just -v lint
      interactive: true
skip_output:
  - meta
  - summary
