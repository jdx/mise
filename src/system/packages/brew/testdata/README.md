# Homebrew receipt fixtures

These files were captured verbatim, read-only, from existing Homebrew installs
on 2026-08-12. No package or cask was installed, removed, or changed.

- `ada-url-INSTALL_RECEIPT.json` and `ada-url-sbom.spdx.json`: Homebrew
  `6.0.17-4-gb94d9e9`; formula `ada-url 4.0.0`.
- `codex-INSTALL_RECEIPT.json`, `codex-config.json`, and `codex.json`:
  Homebrew `6.0.17-4-gb94d9e9`; cask `codex 0.147.0`.

Generating command output:

```text
Homebrew 6.0.17-4-gb94d9e9
```
