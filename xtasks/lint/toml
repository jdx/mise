#!/usr/bin/env bash
#MISE description="Lint TOML files"
set -euo pipefail

toml-sort --check settings.toml --spaces-indent-inline-array 4
toml-sort --check registry.toml --spaces-indent-inline-array 4
