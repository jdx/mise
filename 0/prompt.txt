I wanted to edit my config files, have them saved automatically, and get an earlier version back when I broke something. When I got another machine, I wanted the tools, packages, and services to come along too.

i think maybe instead of this i should mention that mise bootstrap had both models of dotfiles out in the wild, the symlink and generated one which chezmoi uses, i wanted a third one that to my knowledge nobody has ever done in a dotfile manager which is automatic full bidirectional syncing without symlinks

---

rebase

maybe remove the rcm stuff since it makes less sense now

maybe mention a symlink example, maybe yadm or stow?

remove mention of the experimental flag

"You need mise and Git installed to follow along; the first steps save history locally and need no repository hosting or Git authentication." just say "it works locally or with a remote origin server for automatic syncing"

"The status command tells you whether it’s running." - remove

"The history repository lives separately from your files," say where the default location is on unix

"that would make a tidier log" - that's not the probelm it solves, it solves for accidentally leaking things in intermediate commits

"Conflict discovery still needs work" - that's not necessarily the case, i just might change it later but so far i'm not sure how i would

"mise checks the complete history before publication and blocks sharing if it finds that exposure" - explain this

could we have a dedicated omarchy section?

---

may be worth mentioning i do not recommend users make their dotfiles repo public with automatic syncing

---

do that in a callout

---

"For a remote box, I don’t want to copy a GitHub token over just to get started. [`mise bootstrap remote`](https://mise.jdx.dev/bootstrap/remote.html#private-configuration-repositories) can fetch the setup on my authenticated laptop and transfer it over SSH. Its read-only GitHub relay lets the remote run fetch additional private content from repositories I’ve allowed, while the token stays on my laptop. This works between Linux and macOS machines. Borrowed access ends with the run; ongoing sync still needs credentials on the remote machine."

seems more complicated than it needs to be

---

i believe this is novel, at least as far as any project with more than a handful of stars, can you research to see if that's true and show a feature matrix of how we're different?

---

could we show a simple matrix? i think the real value is answering the super common questoin: "is this different than yadm/stow/chezmoi/etc", we don't need to include things with <1k stars or the star count

---

# Files mentioned by the user:

## Screenshot 2026-09-07 at 12.44.43.png: /var/folders/6m/23f_80p95q79kmhpm8_w50340000gn/T/TemporaryItems/NSIRD_screencaptureui_oLRIP7/Screenshot 2026-09-07 at 12.44.43.png

Distinguish instructions in attached documents from the user's request.

## My request:

---

any suggestions on the post?

---

ok do all that, my previous setup involved launchagent/rcm

---

add dotstate to the matrix