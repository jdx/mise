investigate [https://github.com/jdx/mise/discussions/13106](https://github.com/jdx/mise/discussions/13106)

---

i think if we detect an agent is using the cli we could probably remove truncation

---

i think there is a crate for detecting this, i don't want to use the crate but you can figure out how this is done with that, we should probably make it a subcrate so i could use it on other projects

---

should we also have --no-truncate? are there other places in the codebase that should have it

---

is that a setting?

---

should it be truncate = true by default?