description = "Policy enforcement for your pipelines"
os = ["linux", "macos"]
test = { cmd = "conform version", expected = "conform version v{{version}}" }
version_order = "source"

[[backends]]
full = "aqua:siderolabs/conform"

[backends.options]
prerelease = true

[[backends]]
full = "asdf:skyzyx/asdf-conform"

[backends.options]
prerelease = true
